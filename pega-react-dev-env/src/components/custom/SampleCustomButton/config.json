{
  "name": "SampleCustomButton",
  "type": "widget",
  "subType": "button",
  "label": "Sample Custom Button",
  "description": "A custom button component for Pega Constellation applications",
  "organization": "MyOrg",
  "library": "CustomComponents",
  "version": "1.0.0",
  "properties": [
    {
      "name": "label",
      "label": "Button Label",
      "type": "string",
      "required": true,
      "defaultValue": "Click Me",
      "description": "The text displayed on the button"
    },
    {
      "name": "variant",
      "label": "Button Variant",
      "type": "select",
      "required": false,
      "defaultValue": "primary",
      "options": [
        { "value": "primary", "label": "Primary" },
        { "value": "secondary", "label": "Secondary" },
        { "value": "danger", "label": "Danger" }
      ],
      "description": "The visual style variant of the button"
    },
    {
      "name": "size",
      "label": "Button Size",
      "type": "select",
      "required": false,
      "defaultValue": "medium",
      "options": [
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" }
      ],
      "description": "The size of the button"
    },
    {
      "name": "disabled",
      "label": "Disabled",
      "type": "boolean",
      "required": false,
      "defaultValue": false,
      "description": "Whether the button is disabled"
    },
    {
      "name": "onClick",
      "label": "Click Action",
      "type": "action",
      "required": false,
      "description": "Action to perform when the button is clicked"
    }
  ],
  "events": [
    {
      "name": "onClick",
      "label": "On Click",
      "description": "Triggered when the button is clicked"
    }
  ],
  "category": "Input",
  "tags": ["button", "custom", "interactive"]
}

